# Django
SECRET_KEY=change-me-to-a-random-secret-key
DEBUG=false
ALLOWED_HOSTS=localhost,example.com

# Timezone
TZ=Asia/Shanghai

# External model services callback URL (must be reachable by inference services)
INFERS_BASE_URL=http://localhost

# Optional: interactive session token TTL in seconds (default 1800)
# INTERACTIVE_SESSION_TOKEN_TTL_SECONDS=1800
